{
  "name": "yes-chef-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Model Context Protocol server for yes-chef — lets MCP clients (Claude Desktop/Code) plan lunches, log meals, get recommendations, and generate dish images.",
  "type": "module",
  "bin": {
    "yes-chef-mcp": "server.js"
  },
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
